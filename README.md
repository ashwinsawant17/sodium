# sodium

A small foray into messaging apps in C++
Includes a sodium-client and a sodium-server, which can be configured to communicate with each other
Build using CMake
cd into the `sodium` repository
run `cmake -S . -B build`
then run `cmake --build build` to compile
